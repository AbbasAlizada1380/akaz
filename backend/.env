# Database configuration
DB_HOST=localhost          # or your database server IP/hostname
DB_NAME=akaz               # optional, but you can change it
DB_USER=root               # optional, change if needed
DB_PASSWORD=newapp         # optional, change if needed

# Existing variables (keep as is)
VITE_BASE_URL=http://localhost:8038
# VITE_BASE_URL=https://cargob.tet-soft.com


MYSQL_BIN_DIR=C:\Program Files\MySQL\MySQL Server 8.0\bin
JWT_SECRET=akaz