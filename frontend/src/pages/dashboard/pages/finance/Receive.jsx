
import React from "react";

const Receive = ({
  filterYear,
  setFilterYear,
  filterMonth,
  setFilterMonth,
  years, // Receive years array as prop
  months, // Receive month names array as prop
}) => {
  return (
    <div className="bg-gray-100 p-4 rounded-lg shadow mb-6 flex flex-col md:flex-row justify-center items-center gap-4">receive</div>
  );
};

export default Receive;
